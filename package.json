{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=sever && (cd server && npm install) || (cd client && npm install && npm run build)",
    "server": "node server/server.js",
    "client": "cd client && npm run start"
  },
  "author": "Elroy",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
